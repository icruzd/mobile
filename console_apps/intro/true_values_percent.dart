//Descripcion:  Write a method that returns the percentage of the number of elements that have the value true in an array of booleans.

//main() =>  trueValuesPercent([true, false, true, true, true]);
main() =>  containsHello("none", "three", "Hello"]);


int trueValuesPercent(List values) {
   for (String i in values) {
    if (i == "Hello") {
      return true;
    }
    print(i);
  }
 return false;
}
